# Simple Java Socket App

This is a small project I made to practice working with server sockets in Java.

To use it, you need to run at least one server instance. Then you can start as many client instances as you want, each connecting to the server.

Clients can close their connection at any time by sending a single dot (.).

The goal of this project is just to get familiar with basic client–server communication, not to build anything complex.
